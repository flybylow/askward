# create-next-app scaffold assets (archived)

These SVGs shipped with the original Next.js starter and are **not used** in the ask-ward UI.

| File | Notes |
|------|--------|
| `next.svg` | Default Next.js logo |
| `vercel.svg` | Vercel mark |
| `file.svg`, `globe.svg`, `window.svg` | Starter demo icons |

The live app favicon uses `public/hero-seal.png` (see `src/app/layout.tsx` metadata).

Safe to delete this folder if you no longer need the reference.
