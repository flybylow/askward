# Server-only — never expose to the browser
ELEVENLABS_API_KEY=

# Public — used by the browser to identify the agent
NEXT_PUBLIC_ELEVENLABS_AGENT_ID=

# WhatsApp escape hatch (wa.me link)
NEXT_PUBLIC_WHATSAPP_URL=
